from django.urls import path
from . import views

urlpatterns = [
    # Authentication URLs will be added here
    # Example: path('login/', views.login_view, name='login'),
    # Example: path('register/', views.register_view, name='register'),
    # Example: path('logout/', views.logout_view, name='logout'),
]
